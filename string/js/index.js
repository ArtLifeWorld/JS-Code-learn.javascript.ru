"use strict";// Сложение ценlet result = 0.1 + 0.2;alert(+result.toFixed(10) + '$');// Интерфейс суммыconst x = prompt('Введите ваше первое число ?');const y = prompt('Введите ваше второе число ?');if (isNaN(x, y)) {    alert("Строка преобразовалась в NaN. Не число");} else {    alert(+x + +y);}//Как получить дробную часть числа?function getDecimal(num) {    return Math.abs    (Math.round((num - (num ^ 0)) * 1000) / 1000);}alert(getDecimal(12.345)); // 0.345alert(getDecimal(1.2)); // 0.2alert(getDecimal(-1.2)); // 0.2//Случайное из интервала (0, max) / Случайное из интервала (min, max) / Случайное целое от min до max// Min and Max valueconst min = 0,    max = 10;//1.Случайное из интервала (0, max)alert(Math.random() * max)//2.Случайное из интервала (min, max)alert(Math.random() * (max - min) + min);//3.Случайное целое от min до maxfunction randomInteger(min, max) {    return Math.floor(Math.random() * (max - min + 1)) + min;}alert(randomInteger(min, max));