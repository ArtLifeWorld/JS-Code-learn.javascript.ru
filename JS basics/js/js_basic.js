'use strict';// Привет, мир! / Выведите alertalert ('Привет Мир!');// Переменные / Работа с переменнымиvar admin;var name;var name = 'Василий';var admin = name;alert(admin);// Правильный выбор имени переменной / Объявление переменныхlet planetName = 'Земля';let userName = 'Петя';// Побитовые операторы/Проверка, целое ли числоfunction isInteger(num) {    return (typeof num === 'number') && (num % 1 === 0);}alert( isInteger(1) ); // truealert( isInteger(1.5) ); // falsealert( isInteger(-0.5) ); // false// Условные операторы: if, '?' / Проверка стандартаvar officialName = prompt ('Каково «официальное» название JavaScript?', '');    if (officialName == 'ECMAScript') {         alert('Верно!');    } else {        alert('Не знаете? «ECMAScript»!');    }// Взаимодействие с пользователем: alert, prompt, confirm / Простая страницаlet userName = prompt('Please,enter yor name');alert('You name is ' + userName + ' !');// Взаимодействие с пользователем: alert, prompt, confirm / Получить знак числаvar value = prompt('Enter your value', 0);if (value > 0) {    alert( 1 );} else if (value < 0) {    alert( -1 );} else {    alert( 0 );}// Взаимодействие с пользователем: alert, prompt, confirm /Проверка логинаvar accessUser  = prompt('Введите ваш логин', '');if ( accessUser == 'Админ') {    var pass = prompt ('Введите пароль', '');    if ( pass == ('Черный Властелин') ) {       alert('Добро пожаловать');    } else if ( pass == null ) {        alert( 'Вход отменён');    } else {        alert( 'Неправильный пароль' );    }}   else if ( accessUser == null ) {    alert( 'Отмена входа' );} else {    alert( 'Неверный логин' );}// Взаимодействие с пользователем: alert, prompt, confirm / Перепишите 'if' в '?'result = (a + b < 4) ?  'Мало' : 'Много';// Взаимодействие с пользователем: alert, prompt, confirm / Перепишите 'if..else' в '?'let message = (login == 'Вася', '') ? 'Привет' :    (login == ' Дириектор') ? 'Здравствуйте' :        (login == '') ? 'Нет логина' : '' ;// Логические операторы / Проверка if внутри диапазонаif (age >= 14 && age <= 90) {    alert( 'Check!' );}// Логические операторы / Проверка if вне диапазонаif !((age <= 14 && age > 90)) {    alert( 'if вне диапазона v1!' ); }if ((age < 14 || age > 90)) {    alert( 'if вне диапазона v2!' ); }// Функции / Перепишите функцию, используя оператор '?' или '||'// V1function checkAge(age) {    return  =  (age > 18) ?  true :     confirm('Родители разрешили?');    }// v2function checkAge(age) {    return = (age > 18) || confirm('родители разрешили?');}// Функции / Функция minfunction getMin(a,b) {    return (Math.min(a,b));}console.log(getMin(2,1));console.log(getMin(2,5));console.log(getMin(3,-1));console.log(getMin(1,1));// Функции / Функция pow(x,n)function getPow(x,n) {    return Math.pow(x,n);}let x = +prompt ('Введите число', '');let n = +prompt ('Введите степень возведения', '');if (n < 1) alert('Введите целое значение от 1 и выше');console.log(getPow(x,n));// Рекурсия, стек / Вычислить факториалlet n = prompt('Введите число', '100');function getFactorial(n) {    return n ? n * getFactorial(n - 1) : 1;}console.log(getFactorial(n));