"use strict";//  Сложение ценlet result = 0.1 + 0.2;console.log(+result.toFixed(10) + '$');//  Интерфейс суммыconst x = prompt('Введите ваше первое число ?');const y = prompt('Введите ваше второе число ?');if (isNaN(x, y)) {    console.log("Строка преобразовалась в NaN. Не число");} else {    console.log(+x + +y);}//  Как получить дробную часть числа?function getDecimal(num) {    return Math.abs    (Math.round((num - (num ^ 0)) * 1000) / 1000);}console.log(getDecimal(12.345)); // 0.345console.log(getDecimal(1.2)); // 0.2console.log(getDecimal(-1.2)); // 0.2//  Случайное из интервала (0, max) / Случайное из интервала (min, max) / Случайное целое от min до maxconst min = 0, // Min and Max value    max = 10;console.log(Math.random() * max) ;                      //1.Случайное из интервала (0, max)console.log (Math.random() * (max - min) + min);        //2.Случайное из интервала (min, max)function randomInteger(min, max) {                       //3.Случайное целое от min до max    return Math.floor(Math.random() * (max - min + 1)) + min;}console.log(randomInteger(min, max));